.DS_Store
/build/
/engine/target/
/macos/.build/
/macos/.swiftpm/
*.xcuserstate

# Website assets kept locally, not in the repo
/bg.mp4
/.claude/

# Private notes, migrations and infrastructure docs (never pushed)
/_internal/
